{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "bundle exec rails db:prepare && bundle exec puma -C config/puma.rb -b 0.0.0.0",
    "healthcheckPath": "/up",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
