<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "YT Preview - Admin" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="h-full bg-gray-50">
    <nav class="bg-white shadow-sm border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-14 items-center">
          <div class="flex items-center gap-6">
            <%= link_to "YT Preview", admin_videos_path, class: "text-lg font-bold text-red-600" %>
            <% if current_admin %>
              <%= link_to "Videos", admin_videos_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
            <% end %>
          </div>
          <div class="flex items-center gap-4">
            <% if current_admin %>
              <%= link_to admin_account_path, class: "text-sm text-gray-500 hover:text-gray-700 flex items-center gap-1.5" do %>
                <% if current_admin.avatar_url %>
                  <img src="<%= current_admin.avatar_url %>" class="w-6 h-6 rounded-full" alt="" referrerpolicy="no-referrer">
                <% end %>
                <%= current_admin.yt_username %>
              <% end %>
              <%= button_to "Log out", admin_logout_path, method: :delete, class: "text-sm text-gray-500 hover:text-gray-700" %>
            <% else %>
              <%= link_to "Log in", admin_login_path, class: "text-sm text-gray-600 hover:text-gray-900" %>
              <%= link_to "Sign up", admin_signup_path, class: "text-sm bg-red-600 text-white px-3 py-1.5 rounded-md hover:bg-red-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if flash[:notice] %>
        <div class="mb-4 p-3 bg-green-50 border border-green-200 text-green-800 rounded-lg text-sm">
          <%= flash[:notice] %>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="mb-4 p-3 bg-red-50 border border-red-200 text-red-800 rounded-lg text-sm">
          <%= flash[:alert] %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
