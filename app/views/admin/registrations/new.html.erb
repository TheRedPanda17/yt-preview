<div class="max-w-md mx-auto mt-12">
  <h1 class="text-2xl font-bold text-gray-900 mb-6 text-center">Create your account</h1>

  <%= form_with model: @admin, url: admin_signup_path, scope: :admin_user, class: "space-y-4" do |f| %>
    <% if @admin.errors.any? %>
      <div class="p-3 bg-red-50 border border-red-200 text-red-800 rounded-lg text-sm">
        <ul class="list-disc list-inside">
          <% @admin.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <%= label_tag :invite_code, "Invite Code", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= text_field_tag :invite_code, nil, required: true, autocomplete: "off", placeholder: "Enter your invite code", class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border" %>
      <p class="mt-1 text-xs text-gray-500">Required. Get this from the person who set up this app.</p>
    </div>

    <hr class="my-2">

    <div>
      <%= f.label :email, class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.email_field :email, required: true, autofocus: true, class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border" %>
    </div>

    <div>
      <%= f.label :password, class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.password_field :password, required: true, class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border" %>
    </div>

    <div>
      <%= f.label :password_confirmation, "Confirm password", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.password_field :password_confirmation, required: true, class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border" %>
    </div>

    <hr class="my-2">

    <div>
      <%= f.label :yt_username, "YouTube Channel Name", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :yt_username, required: true, placeholder: "e.g. MrBeast", class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border" %>
      <p class="mt-1 text-xs text-gray-500">This will appear in video previews as the channel name.</p>
    </div>

    <div>
      <%= f.label :profile_picture, "Profile Picture", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.file_field :profile_picture, accept: "image/*", class: "w-full text-sm text-gray-500 file:mr-3 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-medium file:bg-red-50 file:text-red-700 hover:file:bg-red-100 file:cursor-pointer" %>
      <p class="mt-1 text-xs text-gray-500">Upload your YouTube channel profile picture.</p>
    </div>

    <div>
      <%= f.label :yt_profile_picture_url, "Or Profile Picture URL", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.url_field :yt_profile_picture_url, placeholder: "https://...", class: "w-full rounded-lg border-gray-300 shadow-sm focus:border-red-500 focus:ring-red-500 px-3 py-2 border" %>
      <p class="mt-1 text-xs text-gray-500">Alternatively, paste a URL. Upload above takes priority.</p>
    </div>

    <div>
      <%= f.submit "Create Account", class: "w-full bg-red-600 text-white py-2.5 rounded-lg font-medium hover:bg-red-700 transition cursor-pointer" %>
    </div>
  <% end %>

  <p class="mt-4 text-center text-sm text-gray-500">
    Already have an account? <%= link_to "Log in", admin_login_path, class: "text-red-600 hover:text-red-700 font-medium" %>
  </p>
</div>
